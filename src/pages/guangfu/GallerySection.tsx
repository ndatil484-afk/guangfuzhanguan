/**
 * Chapter 7's narrative is fully driven by MaterialsSection (蜂窝 → 光板 → 光球).
 * This file is kept as a no-op so the GuangfuPage chapter slot composes without
 * touching the parent — to be removed when the parent is cleaned up.
 */
export default function GallerySection() {
  return null;
}
